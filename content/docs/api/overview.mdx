# API Overview

- Base URL: `https://api.codimir.dev` (SaaS) or your self-host URL
- Auth: Bearer token in `Authorization` header
- Formats: JSON requests/responses, RFC 3339 timestamps

This section will include endpoints for Auth, Tasks, Projects, Events, and Webhooks.
